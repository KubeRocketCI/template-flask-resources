apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-resources.fullname" . }}
  labels:
    {{- include "flask-resources.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-resources.selectorLabels" . | nindent 4 }}
